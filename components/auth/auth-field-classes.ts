/**
 * Shared auth-form field styles. These now defer to the global component
 * classes in `app/globals.css` (.cs-input / .cs-label / .cs-btn-*) so the auth
 * flow stays visually in sync with the landing page and the rest of the app.
 */
export const authTextInputClass = "cs-input";

export const authLabelClass = "cs-label";

export const authPrimaryButtonClass = "cs-btn cs-btn-primary w-full";

export const authSecondaryButtonClass = "cs-btn cs-btn-secondary w-full";
